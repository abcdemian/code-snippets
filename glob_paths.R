# get the full path to every file that has .csv extension located at some predefined folder/path
paths <- Sys.glob(paths = "C:/Users/grams/myfavoritefolder/*.csv")

# obviously you can give the `paths` argument in many different ways
